version: "2.0"
# This file contains the different endpoints your bot can use.

# Server where the models are pulled from.
# https://rasa.com/docs/rasa/user-guide/running-the-server/#fetching-models-from-a-server/

#models:
#  url: http://my-server.com/models/default_core@latest
#  wait_time_between_pulls:  10   # [optional](default: 100)

# Server which runs your custom actions.
# https://rasa.com/docs/rasa/core/actions/#custom-actions/

action_endpoint:
 url: "http://localhost:5056/webhook"

# Tracker store which is used to store the conversations.
# By default the conversations are stored in memory.
# https://rasa.com/docs/rasa/api/tracker-stores/


#event_broker:
    #type: SQL
    #dialect: "postgresql"
    #url: "rasadb.cw9jeev4jo7s.eu-central-1.rds.amazonaws.com"
    #port: 5432
    #db: "rasa"
    #login_db: "postgres"
    #username: "postgres"
    #password: "kruksAnsanta11"

#    use_ssl: <whether or not the communication is encrypted, default false>

tracker_store:
    type: mongod
    url: "mongodb+srv://sangril:kruksAnsanta11@rasadb.dfgvb.mongodb.net/myFirstDatabase?retryWrites=true&w=majority" 
    db: "rasa"
    username: "sangril"
    password: "kruksAnsanta11"

# Event broker which all conversation events should be streamed to.
# https://rasa.com/docs/rasa/api/event-brokers/

#event_broker:
#  url: localhost
#  username: username
#  password: password
#  queue: queue
